# 설계 원칙

이 프로젝트의 코드는 아래 원칙 위에서 판단한다.
원칙은 무엇을 지킬지만 정한다. 어떤 파일·이름·문법으로 지킬지는 매 상황에서 원칙에 비추어 스스로 정한다.

이 언어는 대부분을 막아 주지 않는다. 규율이 곧 유일한 경계다.

## 객체

- 객체는 데이터를 담는 그릇이 아니라 행위의 주체다. 데이터와 그 데이터를 다루는 규칙은 한 객체 안에 있다.
- 객체에게 상태를 묻고 바깥에서 대신 판단해 주는 코드가 보이면, 그 판단을 객체 안으로 옮긴다. 묻지 말고 시킨다.
- 무엇이 안이고 무엇이 밖인지 언어가 강제하지 않는다. 그러므로 공개할 것을 고르는 일이 설계의 일부다. 열려 있다는 것과 써도 된다는 것은 다르다.
- 객체가 자기 규칙을 스스로 지킬 수 없다면, 그 규칙에 필요한 데이터를 자기가 갖고 있지 않다는 뜻이다. 경계를 다시 그어야 한다는 신호다.
- 여러 객체가 협력해야만 성립하는 규칙만 그들 바깥에 둔다. 그 경우에도 판단은 여전히 도메인의 언어로 쓴다.
- 조건 분기가 대상의 종류를 따라 갈라지고 있으면, 그 갈래는 분기가 아니라 각자가 스스로 답하는 형태로 표현될 수 있는지 먼저 본다.
- 무엇이 될 수 있는지를 정하는 것은 상속 관계가 아니라 할 수 있는 일의 목록이다. 필요한 것은 능력이지 혈통이 아니다.

## 도메인 모델

- 모델은 업무를 그대로 옮긴 것이다. 업무 전문가가 쓰는 말과 코드의 말이 일치해야 한다.
- 정체성으로 구분되는 것과 값으로 구분되는 것을 나눈다. 둘은 같음의 정의도, 수명도, 변경 가능성도 다르다.
- 의미가 있는 값은 원시 타입으로 떠돌지 않는다. 같은 원시 타입인데 뜻이 다른 두 값은 서로 섞일 수 있으면 안 된다.
- 이름 없는 자료 뭉치로 도메인 개념을 대신하지 않는다. 무엇이 들었는지 열어 봐야 아는 자료구조는 규칙이 붙을 자리를 만들지 못하고, 필드 이름 하나 바꾸는 일이 어디까지 번지는지 아무도 모르게 만든다.
- 값에 이름을 주면 그 값에 대한 규칙이 붙을 자리가 생긴다. 이름 없는 값에는 규칙이 붙지 못하고 사용처마다 흩어진다.
- 함께 유효해야 하는 것들의 묶음이 하나의 일관성 단위다. 단위 안은 항상 정합하고, 단위 사이는 잠깐 어긋날 수 있다.
- 한 번의 작업은 하나의 일관성 단위를 바꾼다. 한 작업이 여러 단위를 동시에 바꿔야 한다면 단위를 잘못 그은 것이다.
- 단위 바깥의 것은 직접 붙들지 않고 가리키기만 한다. 직접 붙들면 두 단위가 한 덩어리가 된다.
- 단위를 통째로 꺼내고 통째로 저장하는 통로는 단위마다 하나다.
- 업무의 흐름을 조율하는 자리와 업무의 규칙을 판단하는 자리는 다르다. 조율하는 자리에 규칙이 쌓이면 모델이 비어 간다.

## 불변식

- 절대 깨지면 안 되는 조건은 그것을 깰 수 있는 모든 경로에서 막는다. 한 경로에서만 검사하면 나머지 경로가 곧 버그다.
- 타입을 적는 일은 의도를 말하는 것이지 실행을 막는 것이 아니다. 반드시 막아야 하는 것은 값이 태어나는 자리에서 막는다.
- 객체는 태어나는 순간부터 유효해야 한다. 만들어 놓고 나중에 채우는 순서는 유효하지 않은 중간 상태를 허용한다.
- 한 번 정해진 값은 바뀌지 않게 만든다. 바뀔 수 있는 값을 여러 곳이 나눠 가지면 불변식은 결국 아무도 지키지 않는다.
- 기본값은 호출할 때마다 새로 생겨야 한다. 여러 호출이 몰래 나눠 쓰는 기본값은 상태를 실어 나르는 통로가 된다.
- 조건이 깨지면 조용히 넘기지 않는다. 실패는 즉시, 무엇이 왜 어긋났는지와 함께 드러난다.

## 계층과 배치

- 계층을 나누는 목적은 파일을 정리하는 것이 아니라 변경의 파급을 끊는 것이다. 파급이 끊기지 않는 분리는 분리가 아니다.
- 배치는 기술 종류가 아니라 업무 관심사를 먼저 따른다. 같은 이유로 함께 바뀌는 것이 함께 있어야 한다.
- 불러오는 것만으로 무언가 일어나서는 안 된다. 읽어 들이는 일과 실행하는 일은 다르다.
- 서로를 가리키는 두 자리는 경계가 잘못 그어진 것이다. 순서를 바꾸거나 늦춰서 넘기지 말고, 경계를 다시 긋는다.
- 묶음의 바깥에서 쓸 수 있는 것이 무엇인지 그 묶음이 직접 밝힌다. 안쪽 어디에 무엇이 있는지가 바깥의 관심사가 되면 안쪽을 옮길 자유가 사라진다.

## 경계

- 시스템의 각 경계에서 데이터의 표현은 바뀐다. 바깥 세계의 모양이 안쪽까지 그대로 흘러 들어오면 바깥이 바뀔 때 안쪽이 함께 무너진다.
- 안쪽의 모양이 바깥으로 그대로 새어 나가면, 안쪽을 고칠 자유가 사라진다. 외부에 노출한 모양은 곧 계약이다.
- 안에서 쓰는 자료구조를 밖으로 그대로 건네지 않는다. 건넨 순간 그것은 계약이 되고, 받은 쪽이 안쪽을 바꿀 수 있게 된다.
- 경계를 넘는 지점에서 변환한다. 변환은 명시적이고 한 곳에서 일어난다.
- 입력은 경계에서 신뢰할 수 없는 것으로 취급한다. 형식이 맞는지와 업무적으로 타당한지는 다른 질문이고, 답하는 위치도 다르다.
- 바깥에서 들어온 값은 경계를 넘는 순간 이 시스템의 말로 바뀌어 있어야 한다. 안쪽까지 들고 가서 그때 확인하면 이미 늦다.

## 도구와의 관계

- 표준으로 이미 들어 있는 것을 먼저 본다. 자료구조·시간·경로·직렬화처럼 답이 정해진 문제는 대개 이미 풀려 있다.
- 되풀이되는 형식적 코드는 사람이 쓰지 않는다. 사람이 쓰는 것은 결정이고, 형식은 도구가 쓴다.
- 업무 규칙은 어떤 도구에도 위임하지 않는다. 도구는 규칙을 실어 나를 뿐, 규칙을 대신 정하지 않는다.
- 도구가 강요하는 모양이 모델을 왜곡하기 시작하면, 그 도구는 경계 바깥으로 밀어낸다.
- 새 의존을 들일 때는 그것이 없앨 코드와 그것이 들여올 결합을 함께 센다. 한 번 쓰고 말 것은 들이지 않는다.
- 언어가 허용하는 재주와 써도 되는 재주는 다르다. 실행 중에 구조를 바꾸는 기교는 읽는 사람에게서 근거를 빼앗는다.

## 규모에 맞는 구조

- 구조는 문제의 크기를 따라간다. 지금 없는 요구를 위해 미리 세운 층은 비용만 남는다.
- 간접층은 그것이 끊어 주는 파급이 실재할 때만 세운다. 끊을 파급이 없는 층은 읽는 사람의 부담일 뿐이다.
- 다른 언어의 격식을 그대로 옮겨 오지 않는다. 거기서 필요했던 층이 여기서도 필요한 것은 아니다.
- 단순한 것은 단순하게 둔다. 모든 개념에 같은 무게의 격식을 강요하지 않는다. 값 하나로 충분한 자리에 구조를 세우지 않는다.
- 나누는 시점은 커졌다는 신호가 나타난 순간이다. 미리도 아니고, 한참 뒤도 아니다.
- 규칙을 적용할지 말지 판단이 서지 않으면, 그 규칙이 막으려는 사고가 이 규모에서 실제로 일어나는지 묻는다.

## 코드가 스스로 말한다

- 설명을 덧붙이는 주석은 쓰지 않는다. 주석이 필요하다고 느끼는 지점은 이름이나 경계가 틀린 지점이다.
- 공개한 것의 계약을 적는 자리는 따로 있다. 거기에도 무엇을 하는지가 아니라 무엇을 약속하고 무엇을 어길 때 어떻게 되는지를 적는다.
- 무엇을 하는지 설명해야 한다면 이름을 고친다. 왜 그런지 설명해야 한다면 그 이유를 타입이나 구조로 드러낸다.
- 이름은 도메인에서 쓰는 말 그대로 쓴다. 기술 용어로 도메인 개념을 덮지 않는다.
- 축약하지 않는다. 짧은 이름이 아니라 정확한 이름을 고른다.
- 무엇이 오고 가는지 시그니처만 보고 알 수 있어야 한다. 무엇이든 될 수 있다는 표시는 아무것도 말하지 않는 것과 같다.
- 조건식이 무엇을 뜻하는지 읽어서 알 수 없으면, 그 조건에 이름을 준다.
- 읽는 순서가 곧 이해하는 순서가 되게 쓴다. 되짚어 올라가야 이해되는 배치는 다시 배치한다.

## 책임·역할·추상 수준의 정합

- 이 규율은 코드에만 적용되지 않는다. 묶음·파일·타입·함수·이름 등 작성되는 모든 것에 같이 적용된다.
- 하나의 단위는 한 가지 이유로만 바뀐다. 바뀔 이유가 둘이면 둘로 나뉘어야 한다.
- 이름이 가리키는 범위와 실제로 담긴 내용의 범위가 일치해야 한다. 이름보다 많이 하는 단위는 이름이 거짓말을 하는 것이다.
- 한 단위 안의 서술은 같은 추상 수준을 유지한다. 의도를 말하는 문장과 기계를 다루는 문장이 한 자리에 섞이면 읽는 사람이 매번 층을 오르내린다.
- 같은 층에 놓인 것들은 같은 크기의 개념이어야 한다. 한 자리에 큰 개념과 작은 개념이 나란히 있으면 층이 잘못 그어진 것이다.
- 무엇의 묶음인지 한마디로 말할 수 없는 자리는 묶음이 아니라 잡동사니다. 이름에 잡동사니를 뜻하는 말이 들어가면 그 경계는 실패한 것이다.
- 어떤 이름을 붙여야 할지 오래 고민된다면, 대개 책임이 하나가 아니기 때문이다.

## 실패

- 실패는 값이 아니라 흐름이다. 정상 흐름과 실패 흐름을 한 반환값에 섞어 호출자가 매번 분해하게 만들지 않는다.
- 실패를 뜻하는 빈 값을 돌려주지 않는다. 없음과 잘못됨은 다른 사건이고, 돌려받은 쪽이 그 둘을 구별할 수 없으면 둘 다 놓친다.
- 실패의 종류가 다르면 다루는 방법도 다르다. 요청이 잘못된 것, 규칙에 어긋난 것, 대상이 없는 것, 시스템이 고장난 것을 구별한다.
- 무엇이든 붙잡는 처리는 아무것도 처리하지 않는 것이다. 붙잡을 것을 이름으로 지목한다.
- 각 실패는 그것을 의미 있게 처리할 수 있는 단 한 곳에서 처리한다. 처리할 수 없는 곳에서 붙잡지 않는다.
- 실패의 이름은 무엇이 어긋났는지를 도메인의 말로 말한다. 기술적 오류명으로 업무적 위반을 덮지 않는다.
- 시작한 것은 끝을 보장한다. 도중에 실패해도 정리되도록 시작과 끝을 한 구조로 묶는다.

## 저장

- 저장은 규칙의 결과를 남기는 일이지, 규칙을 결정하는 일이 아니다. 업무 규칙이 저장 기술의 편의를 따라가면 안 된다.
- 무엇을 언제 읽어 오는지는 성능이 아니라 설계의 문제다. 필요한 것을 필요한 만큼, 예측 가능하게 읽는다.
- 하나를 조회할 때 숨어서 여러 번 읽는 구조는 규모가 커지면 반드시 문제가 된다. 읽기 계획은 코드에 드러나야 한다.
- 어디까지 함께 저장되고 어디부터 따로인지가 코드에서 읽혀야 한다.
- 조회 전용 경로와 변경 경로는 요구가 다르다. 같은 모양을 억지로 공유하지 않는다.
